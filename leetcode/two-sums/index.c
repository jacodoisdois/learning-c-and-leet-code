#include <stdio.h>

int* twoSum(int* nums, int numsSize, int target, int* returnSize) {

    for(int i = 0; i < numsSize; i ++) {
        // to-do
    }

    return NULL;
}

int main (){
    int arr[] = {1,2,3,4,5}; 
    int arrSize = sizeof(arr) / sizeof(arr[0]);
    
    twoSum(arr, arrSize, 0, 0);
    return 0;
}